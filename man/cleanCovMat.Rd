% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanCovMat.R
\name{cleanCovMat}
\alias{cleanCovMat}
\title{clenCovMat}
\usage{
cleanCovMat(
  input.obj,
  max_na_cpg = 0.5,
  max_na_ind = 0.2,
  cpg_removal_threshold = 10,
  verbose = FALSE
)
}
\arguments{
\item{input.obj}{SummarizedExperiment assay object (list) as input with list with coverage, methylated counts and unmethylated counts in assays/data/listData (rows are CpGs and individuals are columns).}

\item{max_na_cpg}{Threshold of missing values per each CpG in the coverage counts matrix.}

\item{max_na_ind}{Threshold of missing values per each individual.}

\item{cpg_removal_threshold}{Minimum threshold of coverage counts (across all individuals) for a CpG to be kept.}

\item{verbose}{suppress messages for progression, default is FALSE}
}
\value{
\item{List with 3 elements}{coverage matrix, methylated counts matrix, unmethylated counts matrix, cleaned at the specified missingness thresholds removed.}
}
\description{
\code{}First part of CpGs' cleaning. \code{cleanCovMat} helps in the conversion of zeroes into missing values (0->NA) in the coverage matrix,
removing rows (CpGs) and columns (individuals) above pre-specified missingness threshold.
}
\examples{
library(dplyr)
library(SummarizedExperiment)
data("meth_data")
print(meth_data)
input.list <- SummarizedExperiment::assays(meth_data)
subset_list <- lapply(input.list, function(df) df[1:100, 1:10])
clean.coverage <- cleanCovMat(input.obj = subset_list, max_na_cpg = 0.5,
max_na_ind = 0.2,  cpg_removal_threshold = 10)


}
