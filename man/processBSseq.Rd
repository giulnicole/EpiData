% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processBSseq.R
\name{processBSseq}
\alias{processBSseq}
\title{#' @title processBSseq
Process \code{BSseq} object}
\usage{
processBSseq(bs, verbose = TRUE)
}
\arguments{
\item{bs}{A \code{BSseq} object.}

\item{verbose}{Print detailed output message (default = TRUE)}
}
\value{
Returns the same \code{BSseq} object with zero-coverages assigned to NA and rownames with the genomic positions.
}
\description{
\itemize{
\item Zero-coverage sites are converted to \code{NA} since they are considered as missing data
\item Adds rownames to the \code{BSseq} object
}
}
\examples{

library(bsseq)
data(BS.chr22)

bs1 <- BS.chr22
cov1 <- bsseq::getCoverage(BSseq = bs1, type = "Cov")
head(cov1, n = 10)

bs2 <- processBSseq(bs1)
cov2 <- bsseq::getCoverage(BSseq = bs2, type = "Cov")
head(cov2, n = 10)

}
