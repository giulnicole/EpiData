% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateImputation.R
\name{evaluateImputation}
\alias{evaluateImputation}
\title{evaluateImputation}
\usage{
evaluateImputation(original, imputed)
}
\arguments{
\item{original}{A numeric dataframe or matrix (original complete data)}

\item{imputed}{A numeric dataframe or matrix (imputed data, same dimensions as original)}
}
\value{
A list containing:
\itemize{
\item metrics: Dataframe with RMSE and MAE for each variable
\item overall: Overall RMSE and MAE across all variables
}
}
\description{
evaluateImputation
}
\examples{
# Original complete data
original <- data.frame(
  x1 = rnorm(100),
  x2 = rnorm(100),
  x3 = rnorm(100)
)

# Data after imputation
imputed <- original
imputed[sample(1:100, 20), 1] <- imputed[sample(1:100, 20), 1] + rnorm(20, 0, 0.5)

# Evaluate
results <- evaluateImputation(original, imputed)
print(results$metrics)
print(results$overall)
}
