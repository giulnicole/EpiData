% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cov.R
\name{read_cov}
\alias{read_cov}
\title{Read coverage files as methrix object}
\usage{
read_cov(
  files = NULL,
  sample_names = NULL,
  merge_strand = TRUE,
  verbose = TRUE,
  col_names = c("chr", "start", "end", "coverage", "methylated", "unmethylated")
)
}
\arguments{
\item{files}{Character vector of .cov.gz file paths. If NULL,
uses package's built-in data.}

\item{sample_names}{Character vector of sample names.
If NULL, uses basename of files.}

\item{merge_strand}{Should forward and reverse strands be merged?
Default: TRUE (recommended for most analyses).}

\item{verbose}{Print progress messages? Default: TRUE.}

\item{col_names}{Column names in the .cov.gz files. Default assumes:
c("chr", "start", "end", "coverage", "methylated", "unmethylated")
Set to NULL for automatic detection.}
}
\value{
A methrix object containing methylation data.
}
\description{
This function reads .cov.gz files and creates a methrix object
for efficient methylation analysis.
}
\examples{
\dontrun{
# Read package's example data
mtx <- read_cov()

# Read specific files
files <- c("sample1.cov.gz", "sample2.cov.gz")
mtx <- read_as_methrix(files = files, sample_names = c("S1", "S2"))
}
}
